import React, { FC } from 'react';

import styles from './SystemSettings.module.sass';

export const SystemSettings: FC = () => {
  return <div className={styles.systemsWrapper}>System settings</div>;
};
